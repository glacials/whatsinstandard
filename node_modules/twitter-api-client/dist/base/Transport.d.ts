import IClientOptions from './IClientOptions';
declare class Transport {
    private oauth;
    private cache?;
    private credentials;
    constructor(options: IClientOptions);
    updateOptions(options: Partial<IClientOptions>): void;
    doDeleteRequest<T>(url: string): Promise<T>;
    doGetRequest<T>(url: string): Promise<T>;
    doPostRequest<T>(url: string, body?: any, contentType?: string): Promise<T>;
}
export default Transport;
